# linkedin-ai-slop

Chrome extension + local proxy: while scrolling LinkedIn, posts are scored via the TypeSafe **Jev** API (`POST https://api.typesafe.ai/v1/systemone`) for AI slop.

Private WIP — scaffolding incoming.
